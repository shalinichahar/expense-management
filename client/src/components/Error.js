// import { useRouteError } from "react-router-dom";

const Error = () =>{
    return(
        <div>
            <h1>Opps!</h1>
            <h2>something went wrong!</h2> 
            {/* <h2>{err.status + " : " + err.status}</h2> */}
        </div>
    );
};

export default Error;